import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com','project-management.fra1.digitaloceanspaces.com'],
    
  },
};

export default nextConfig;
